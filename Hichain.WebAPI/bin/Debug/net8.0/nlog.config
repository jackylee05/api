<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      internalLogLevel="Trace"
      internalLogFile="${basedir}/LOG/nlog-internal.log"
      autoReload="true"
      throwConfigExceptions="true">

  <targets>
    <!-- 写日志到运行目录下的 LOG 文件夹，按日期分文件 -->
    <target xsi:type="File"
            name="allfile"
            fileName="${basedir}/LOG/${shortdate}.log"
            createDirs="true"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}" />

    <!-- 同时输出到控制台窗口，方便查看监听端口等信息 -->
    <target xsi:type="Console"
            name="console"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}" />
  </targets>

  <rules>
    <!-- 从 Trace 开始全部写入文件和控制台（与 Program.cs 中的 MinimumLevel.Trace 对应） -->
    <logger name="*" minlevel="Trace" writeTo="allfile,console" />
  </rules>

</nlog>

